node_modules
# Keep environment variables out of version control
.env
.env.staging

# هش‌های SCRAM واقعی PgBouncer — هرگز وارد گیت نشوند (قالب در docker/userlist.txt.example)
docker/userlist.txt

# بیلد و خروجی‌های محلی — هرگز وارد ورژن کنترل نشوند
/dist

# پشتیبان‌های دیتابیس
/backups
*.log

/src/generated/prisma

/src/generated/prisma
