# کارنامهٔ کیفیت اپلیکیشن موبایل — ما انبارداری (mobile/)

> **دامنه:** فقط پوشهٔ `mobile/` (اپ Flutter)
> **هدف سیستم:** انبارداری اختصاصی — حداکثر ۳۰ کاربر و ۲۰٬۰۰۰ محصول
> **مبنای امتیاز:** بازخوانی کد + متریک‌های واقعی استخراج‌شده از سورس
> **مکمل:** `MOBILE-BUGS-LIST.md` (جزئیات هر باگ) و `BUGS-LIST.md` (بک‌اند)

---

## 🎯 امتیاز کلی: **74 / 100** — رتبه **B (خوب، آمادهٔ تولید نیست)**

اپ از نظر معماری و الگوهای احراز هویت **بالاتر از میانگین** است، اما به‌دلیل چند نقطهٔ شکست در **مقیاس واقعی (۲۰هزار محصول)** و **پیکربندی تولید (baseUrl/TLS)**، هنوز برای استقرار تولیدی آماده نیست. با رفع فاز ۱ و ۲ باگ‌لیست، امتیاز به محدودهٔ ۹۰ می‌رسد.

```
معماری و ساختار     ████████████████░░░░  82/100
کیفیت کد            ███████████████░░░░░  73/100
امنیت               ██████████████░░░░░░  70/100
پایداری و ضدکرش     █████████████░░░░░░░  66/100
صحت محاسبات         ████████████████░░░░  80/100
مقیاس‌پذیری          ██████████░░░░░░░░░░  52/100  ← ضعیف‌ترین
تست               ███████████████░░░░░  74/100
تجربهٔ کاربری        ████████████████░░░░  80/100
```

---

## 📊 متریک‌های واقعی (استخراج‌شده از سورس)

| متریک | مقدار | ارزیابی |
|-------|-------|---------|
| فایل‌های Dart غیرتولیدی | ۱۱۸ فایل | ساختار ریزدانه و تمیز ✅ |
| خطوط کد (بدون generated) | ~۱۸٬۱۴۶ | پروژهٔ متوسط، بالغ |
| فایل‌های تست | ۹ فایل / ~۹۰۱ خط | برای پروژهٔ شخصی خوب ✅ |
| چک `mounted` | ۱۳۱ مورد | آگاهی از lifecycle بالا ✅ |
| `catch (_)` (خطای بالقوه خاموش) | **۴۸ مورد** | نگران‌کننده ⚠️ |
| `ListView.builder`/`builder:`/`Sliver` | ۱۰۵ مورد | اکثر لیست‌ها lazy هستند ✅ |
| `withOpacity` (منسوخ) | **۱۴۸ مورد** | بدهی فنی UI ⚠️ |
| pagination در API | **۰ مورد** | نقطهٔ شکست مقیاس 🔴 |

---

## ۱. معماری و ساختار درختی — **82/100** 🟢

**نقاط قوت:**
- ✅ ساختار **feature-first** تمیز: `core/` (زیرساخت) + `features/<role>/` (منطق هر نقش) + `shared/`. هر feature خودش `data / models / providers / screens / widgets` دارد.
- ✅ تفکیک سه نقش (manager / warehouse_keeper / driver) شفاف و بدون درهم‌تنیدگی.
- ✅ لایه‌بندی درست: `ApiService` (Dio) ← `Provider` (Riverpod) ← `Screen`. UI مستقیم به شبکه وصل نیست.
- ✅ استفاده از `freezed` + `json_serializable` برای مدل‌ها (type-safe، immutable).
- ✅ مسیریابی متمرکز و **تست‌پذیر** (`authRedirect` جدا از `GoRouter`).

**نقاط ضعف:**
- ⚠️ **تکرار (duplication):** چند فایل هم‌نام و موازی وجود دارد که سردرگم‌کننده است:
  - `core/notifications/notification_model.dart` و `notification_provider.dart` **و** `features/manager/dashboard/widgets/notification_bell.dart` و `shared/widgets/notification_bell.dart` (دو `notification_bell`!)
  - `shared/settings/settings_screen.dart` **و** `features/manager/.../settings/settings_screen.dart` (دو صفحه تنظیمات + دو `settings_api_service`)
  - `features/manager/dashboard/widgets/notifications_screen.dart` و `features/shared/notifications/notifications_screen.dart`
- ⚠️ عمق پوشه‌ها گاهی زیاد است (مثلاً `manager/dashboard/quick_actions/warehouses/...` تا ۵-۶ سطح).
- ⚠️ `ApiConstants` ناقص است؛ بیشتر مسیرها در `manager_api_service.dart` هاردکد شده‌اند (MM3).

**پیشنهاد:** یک‌بار پاک‌سازی duplicationها + یک `endpoints.dart` واحد.

---

## ۲. کیفیت کد — **73/100** 🟡

**نقاط قوت:**
- ✅ کامنت‌های فارسی مفید و توضیحی (به‌ویژه در `dio_client`, `auth_provider`).
- ✅ الگوی single-flight برای refresh و unlock — سطح حرفه‌ای.
- ✅ `friendlyError` متمرکز برای تبدیل خطا به پیام فارسی — تمیز و قابل استفادهٔ مجدد.
- ✅ استفادهٔ گستردهٔ درست از `ListView.builder` (۱۰۵ مورد).

**نقاط ضعف:**
- 🔴 **۴۸ مورد `catch (_)`** — بعضی خطاها کاملاً بی‌صدا بلعیده می‌شوند (مثل `products_screen.dart:65`) و کاربر صفحهٔ خالی می‌بیند. این بزرگ‌ترین ضعف کیفیتی است.
- 🟠 استفاده از `List<Map<String,dynamic>>` به‌جای مدل typed در `products_screen` (MM4) — خطاهای runtime را دعوت می‌کند در حالی که خود پروژه مدل `ProductModel` دارد.
- 🟠 **۱۴۸ مورد `withOpacity`** منسوخ — در Flutter جدید هشدار می‌دهد؛ باید به `.withValues(alpha:)` مهاجرت کند.
- 🟡 `analysis_options.yaml` فقط `flutter_lints` پایه دارد؛ هیچ rule سخت‌گیرانه‌ای (مثل `require_trailing_commas`, `avoid_dynamic`) فعال نیست.
- 🟡 چند `print` خام به‌جای logger (`socket_service.dart:47`).

---

## ۳. امنیت — **70/100** 🟡

**نقاط قوت:**
- ✅ توکن در **Keystore/Keychain** رمزنگاری‌شده (`flutter_secure_storage`).
- ✅ **رفرش توکن چرخشی** + تفکیک «نشست مرده» از «خطای شبکه» → کاربر با قطعی موقت پرت نمی‌شود.
- ✅ **قفل برنامه** (پین/بیومتریک با `local_auth`).
- ✅ گوش‌دادن به `user:force:logout` برای خروج اجباری از سمت سرور.
- ✅ کلید/سکرت مدل و منطق حساس سمت اپ نیست.

**نقاط ضعف:**
- 🟠 **`baseUrl = http://localhost:3000`** — هم روی دستگاه واقعی کار نمی‌کند، هم **بدون TLS**؛ توکن‌ها plaintext منتقل می‌شوند (MH3/MM6). باید `https` + از `dart-define`.
- 🟡 روی وب، توکن به `SharedPreferences` (نه secure) برمی‌گردد — برای اپ موبایل شرکتی قابل قبول است اما اگر بیلد وب استفاده شود ریسک دارد.
- 🟡 نبود **certificate pinning** (برای سیستم اختصاصی شرکتی توصیه می‌شود).
- 🟡 وابسته به بک‌اند: باگ **H1** (سوکت `tokenVersion` را چک نمی‌کند) روی امنیت realtime موبایل هم اثر دارد.

---

## ۴. پایداری و عدم کرش — **66/100** 🟠

**نقاط قوت:**
- ✅ ۱۳۱ چک `mounted` — آگاهی خوب از lifecycle در بیشتر صفحات.
- ✅ گارد `_loggingOut` و `_processing`/`_submitting` برای جلوگیری از عملیات همزمان.
- ✅ `dispose` درست کنترلرها در اکثر صفحات.

**نقاط ضعف:**
- 🟠 چند نقطهٔ **`setState` بعد از `await` بدون چک `mounted`** (مثل `products_screen.dart:64,66`) → کرش `setState after dispose`.
- 🟠 **نشت listener سوکت** (MH4): singleton سوکت listenerها را انباشته می‌کند و در `dispose` صفحهٔ مدیر off نمی‌شوند → rebuild اضافی و نشت حافظه.
- 🟠 **عدم reconnect سوکت با توکن تازه** (MH5) پس از انقضا.
- 🟠 **Race در `check_in`** (MH6): ویرایش لیست حین ثبت.
- ⚠️ نبود **ErrorBoundary سراسری** / `FlutterError.onError` و `runZonedGuarded` در `main.dart` → کرش‌های پیش‌بینی‌نشده هیچ‌جا لاگ/گزارش نمی‌شوند (نه Crashlytics، نه Sentry).

---

## ۵. صحت محاسبات — **80/100** 🟢

**نقاط قوت:**
- ✅ تبدیل تاریخ شمسی با `shamsi_date` (کتابخانهٔ استاندارد، نه محاسبهٔ دستی).
- ✅ نرخ دلار با `double.tryParse` امن و fallback به `null`.
- ✅ منطق موجودی و مجموع‌ها ساده و مستقیم است (`fold`).
- ✅ تست اختصاصی برای `token_expiry` و `product_inventory_screen`.

**نقاط ضعف:**
- 🟡 محاسبهٔ `_totalUnits` با `fold` در **هر rebuild** (MM1) — درست است اما ناکارآمد.
- 🟡 هم‌سو با باگ **L5** بک‌اند: تبدیل `Decimal → Number` برای قیمت‌ها روی مبالغ بزرگ (ریال/تومان) ریسک دقت اعشاری دارد. برای مبالغ مالی از `Decimal`/رشته استفاده شود.
- 🟡 `_totalUnits` از `toInt()` استفاده می‌کند؛ اگر بک‌اند مقدار اعشاری بدهد گرد می‌شود.

---

## ۶. مقیاس‌پذیری — **52/100** 🔴 (ضعیف‌ترین حوزه)

**این تنها حوزه‌ای است که می‌تواند اپ را در دنیای واقعی زمین بزند.**

- 🔴 **صفر مورد pagination** — `getProducts`/`getOrders` کل دیتا را یک‌جا می‌گیرند. با ۲۰هزار محصول: payload سنگین، ریسک OOM، TTI چند ثانیه‌ای.
- 🔴 flatten کل محصولات×مدل‌ها روی **UI thread** (`products_screen`).
- 🔴 `ListView(children:[...])` و `SingleChildScrollView` نمودار در صفحهٔ موجودی — رندر همهٔ آیتم‌ها.
- 🟠 نبود کش لوکال (مثل Hive/Isar) → هر ورود به صفحه = درخواست کامل مجدد.

**نکته:** بخش زیادی از این به بک‌اند وابسته است (باگ **C2** — CROSS JOIN موجودی). موبایل و بک‌اند باید هماهنگ pagination را اضافه کنند.

---

## ۷. تست — **74/100** 🟢

**نقاط قوت:**
- ✅ ۹ فایل تست با mock حرفه‌ای (mock کانال secure storage، FakeHttpAdapter بدون شبکهٔ واقعی).
- ✅ پوشش نقاط حساس: `auth_session`, `token_expiry`, `app_router`, `lock_provider/storage`.
- ✅ منطق مسیریابی و انقضای توکن به‌صورت واحد تست‌پذیر طراحی شده.

**نقاط ضعف:**
- 🟡 پوشش UI و صفحات سنگین (products/inventory/scan) کم است.
- 🟡 نبود integration test و golden test.
- 🟡 نسبت تست به کد (~۹۰۰ به ~۱۸هزار) پایین است (~۵٪).

---

## 🏁 جمع‌بندی و مسیر رسیدن به «فوق‌العاده»

| اگر انجام شود | امتیاز تخمینی |
|---------------|:---:|
| وضعیت فعلی | **74** |
| + فاز ۱ باگ‌لیست (baseUrl/TLS، mounted، خطای خاموش، سوکت) | **~83** |
| + فاز ۲ (pagination + بهینه‌سازی مقیاس) | **~91** |
| + ErrorBoundary سراسری + Crashlytics + کش لوکال | **~95** |

**سه کار با بیشترین اثر (به ترتیب):**
1. **pagination** (سمت سرور + کلاینت) — بدون آن اپ با دیتای واقعی کرش می‌کند.
2. **baseUrl از dart-define + TLS** — بدون آن اپ روی دستگاه واقعی اصلاً کار نمی‌کند.
3. **حذف ۴۸ مورد `catch(_)` خاموش + ErrorBoundary سراسری** — برای «بدون باگ» بودن و دیدن خطاها.

> این کارنامه فقط سمت `mobile/` را ارزیابی می‌کند. موارد وابسته به بک‌اند در `MOBILE-BUGS-LIST.md` با نشان «هم‌سو با بک‌اند» مشخص شده‌اند.
