<?xml version="1.0" encoding="utf-8"?>
<!--
  پیکربندی امنیت شبکه — ترافیک کلیرتکست (HTTP) به‌طور پیش‌فرض مسدود است.
  فقط localhost برای توسعهٔ محلی مجاز است؛ در تولید با HTTPS کار کنید.
  اگر سرور واقعی HTTP بدون TLS است (توصیه نمی‌شود)، دامنهٔ آن را به‌طور موقت
  به <domain includeSubdomains="true">your-server.ir</domain> اضافه کنید — ولی هرگز کل پایه را cleartext نکنید.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">localhost</domain>
        <domain includeSubdomains="true">10.0.2.2</domain>
        <domain includeSubdomains="true">127.0.0.1</domain>
    </domain-config>
</network-security-config>
